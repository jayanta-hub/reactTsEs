// import { useSelector } from "react-redux";

const UseIsRole = (): string => {
    // const UserInfo = useSelector((state: any) => state?.loginSlice?.userLoginInfo?.data?.token);
    const UserType = "Admin";
    return UserType;
  };
  
  export default UseIsRole;
  